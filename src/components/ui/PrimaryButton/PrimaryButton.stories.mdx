import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {PrimaryButton} from "./PrimaryButton";

<Meta title="ui/PrimaryButton" component={PrimaryButton} />

export const Template = (args) => <PrimaryButton {...args} />;

PrimaryButton

<Canvas>
    <Story
        name="Normal"
        args={{
            children: (<>Button</>)
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>