import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {StudioSection} from "./StudioSection";

<Meta title="container/StudioSection" component={StudioSection} />

export const Template = (args) => <StudioSection {...args} />;

Section that contains specific part of content

<Canvas>
    <Story
        name="Normal"
        args={{
            children: (<>content</>),
            title: "Section title"
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>