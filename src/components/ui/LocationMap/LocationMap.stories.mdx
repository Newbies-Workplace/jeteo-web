import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {LocationMap} from "./LocationMap";

<Meta title="ui/LocationMap" component={LocationMap}/>

export const Template = (args) => <LocationMap {...args} />

Location map component for the event details page.
Do not render it in a view should the coordinates be undefined.

<Canvas style={{background: "silver"}}>
    <Story
        name="With address"
        parameters={{
            layout: 'centered',
        }}
        args={{
            coordinates: {
                lat: 51.08550941185408,
                lng: 17.01043960812176,
            },
            address: 'Racławicka 2/4, 53-146 Wrocław'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas style={{background: "silver"}}>
    <Story
        name="Without address"
        parameters={{
            layout: 'centered',
        }}
        args={{
            coordinates: {
                lat: 51.08550941185408,
                lng: 17.01043960812176,
            },
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas style={{background: "silver"}}>
    <Story
        name="With a very long address"
        parameters={{
            layout: 'centered',
        }}
        args={{
            coordinates: {
                lat: 51.08550941185408,
                lng: 17.01043960812176,
            },
            address: 'Dłuuuuuuuuuuuuuuuuuuuuuuga 2/4, 53-146 Wrocław'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>