import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {NavButton} from "./NavButton";
import {HashRouter} from 'react-router-dom';

<Meta
    title="ui/NavButton"
    component={NavButton}
    decorators={[
        (Story) => (
            <HashRouter>
                <Story />
            </HashRouter>
        )
    ]}
/>

export const Template = (args) => <NavButton {...args} />

navigation button

<Canvas>
    <Story
        name="Active"
        args={{
            children: (<>content</>),
            name: 'Przycisk',
            to: '/'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story
        name="Inactive"
        args={{
            children: (<>content</>),
            name: 'Przycisk',
            to: '/test'
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>