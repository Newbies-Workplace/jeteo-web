import { Canvas, Meta, Story } from '@storybook/addon-docs';
import {StepView} from "./StepView";

<Meta title="ui/StepView" component={StepView} />

export const Template = (args) => <StepView {...args} />;

Just a step component with example steps

<Canvas>
    <Story
        name="Normal"
        args={{
            steps: ["Profil użytkownika", "Znajomi", "Polubione"],
            activeStepIndex: 1
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>