import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {EventSkeleton} from "./EventSkeleton";

<Meta title="molecules/EventSkeleton" component={EventSkeleton} />

export const Template = (args) => <EventSkeleton {...args} />;

EventSkeleton

<Canvas>
    <Story
        name="EventSkeleton"
        parameters={{
            backgrounds: { default: 'white' }
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>