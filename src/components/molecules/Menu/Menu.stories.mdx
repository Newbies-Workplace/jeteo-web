import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {Menu} from "./Menu";
import {useState} from "react";

<Meta
    title="molecules/Menu"
    component={Menu}
/>

Menu example

<Canvas>
    <Story
        name="Simple"
    >
        {() => {
            const onOptionClick = () => {
                setOpen(false)
            }
            const options = [
                {text: 'Edytuj', onClick: onOptionClick},
                {text: 'Usuń', onClick: onOptionClick},
                {text: 'Mleko', onClick: onOptionClick},
            ]
            const [isOpen, setOpen] = useState(true)
            return <div style={{display: 'flex', justifyContent: 'center', alignItems: 'center', width: 500, height: 500}}>
                <button onClick={() => setOpen(!isOpen)}>otwórz menu</button>
                <Menu isOpen={isOpen} setIsOpen={setOpen} options={options}/>
            </div>
        }}
    </Story>
</Canvas>