import {Canvas, Meta, Story} from '@storybook/addon-docs';
import {StudioLectureCard} from "./StudioLectureCard"; import dayjs from "dayjs";

<Meta title="ui/StudioLectureCard" component={StudioLectureCard} />

export const Template = (args) => <StudioLectureCard {...args} />;

Lecture card used in Studio

<Canvas>
    <Story
        name="Active"
        args={{
            title: 'Tytuł prelekcji',
            descriptionSnippet: 'Jakiś podtytuł',
            startDate: new Date(),
            finishDate: new Date(),
            speakers: [
                {name: 'Wiktor'},
                {name: 'Denis'}
            ],
        }}
    >
        {Template.bind({})}
    </Story>
</Canvas>