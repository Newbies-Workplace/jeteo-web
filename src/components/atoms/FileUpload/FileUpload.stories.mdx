import {Canvas, Meta, Story} from '@storybook/addon-docs';
import FileUpload from "./FileUpload";

<Meta title="atoms/FileUpload" component={FileUpload} />

export const Template = (args) => <FileUpload {...args} />;

<Canvas>
    <Story name="WithHint">
        {Template.bind({})}
    </Story>
</Canvas>

<Canvas>
    <Story name="WithoutHint"
        args={{
            hint: null
        }}>
        {Template.bind({})}
    </Story>
</Canvas>